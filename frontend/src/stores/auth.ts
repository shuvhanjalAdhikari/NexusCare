// ================================================================
// NexusCare — Auth Store (Zustand)
// Holds current_user and current_membership for the session.
// ================================================================
//
// TODO(Stage 3): Implement the Zustand auth store.
//   - State: currentUser, currentMembership (hospital_id, role), status.
//   - Actions: setSession, clearSession.
//   - Hydrated after select-workspace; cleared on logout / 401.
//   - Server data belongs in TanStack Query, NOT here — see CLAUDE.md §8.

export {};
