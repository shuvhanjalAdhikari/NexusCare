// ================================================================
// NexusCare — Design Token Helpers
// Optional TypeScript-side access to design tokens.
// ================================================================
//
// TODO(as needed): Add typed helpers only if components need design
// tokens in JS (e.g. chart libraries that cannot read CSS variables).
// Visual styling is otherwise driven entirely by Tailwind utilities
// and the tokens defined in src/app/globals.css — see CLAUDE.md §5.

export {};
